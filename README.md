# Tor Expert Bundle

## Installation

Download and extract the release archive, then launch Tor with the `start-tor` script. All required files will be created in the internal directory.

> [!IMPORTANT]
> - The default listening port is `127.0.0.1:9050`.
> - Uncomment bridges in `torrc` to use them.
